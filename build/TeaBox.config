<?xml version="1.0" encoding="utf-8"?>
<!-- Configuration file generated by Facade 4.8.0 -->
<configuration>
  <appSettings>
    <!-- The version specified in the package version will be used if not specified in command line. Otherwise it will get the latest version available. -->
    <add key="PackageVersion" value="2023.4.4" />

    <!-- The package name can be overridden in the config file. -->
    <add key="PackageName" value="TeaBox.Next" />

    <!-- Specify the repository branch to launch the latest build version from the repository branch. -->
    <!--<add key="RepositoryBranch" value="" />-->

    <!-- Specify the name of the executable to be launched in the package, 
        this value should not be changed most of the time as it is already baked in the executable. -->
    <!--<add key="ExecutableRelativePath" value="" />-->

    <!-- The NuGet source can be overridden in the config file. -->
    <!-- <add key="PackageSource" value="https://artifactory.ubisoft.org/api/nuget/nuget-snapshot" /> -->

    <!--
        Minimal LogLevel for which the Facade will output its own messages to the console.
        Supported values are : Trace, Debug, Information, Warning, Error, None
        For instance, if set to "Warning", the Facade's Trace, Debug and Information messages will not appear in the console.
        Note that this doesn't affect the "facadized" executable's logs; they will always be shown in the console.
    -->
    <!--<add key="VerbosityMinimumLevel" value="Information"/>-->

    <!-- Allow to inject additional arguments when launching the final process, 
        the argument can use the tag %FACADE_LOCATION% to have an argument using the directory of the facade. -->
    <add key="AdditionalArguments" value="-scriptsFolder=%FACADE_LOCATION%/scripts" />

    <!-- Allow to override the run mode of a facade. By default, .net executable are run directly in the same process, 
        allowing a more transparent usage of the facades   allowed values are : Embedded, SeparateProcess, SeparateProcessAsAdmin, EmbeddedWithoutAppDomain -->
    <!--<add key="RunMode" value="SeparateProcess"/>-->

    <!-- By default it's true, we can set to false only when RunMode is in SeparateProcess or SeparateProcessAsAdmin to stop the facade when process is launched -->
    <!--<add key="WaitForExit" value="True"/>-->

    <!-- Force a Working Directory to the application, you can add %PACKAGE_LOCATION% to use the directory of the package. -->
    <add key="WorkingDirectory" value="%FACADE_LOCATION%/.." />

    <!-- Set Environment Variables to the application, of format var1=value1;var2=value2  -->
    <!--<add key="EnvironmentVariables" value=""/>-->

    <!-- If true, application will be copied in a temp folder before being launched. As a result, original files won't be locked.  -->
    <!--<add key="ShadowCopyFiles" value="False"/>-->

    <!-- The Uri where to get the Facade.Implementation -->
    <!--<add key="FacadeImplementationUri" value="https://artifactory.ubisoft.org/generic/Facade/Facade.Implementation$RUNTIME$/Facade.Implementation$RUNTIME$-$VERSION$.zip"/>-->

    <!-- The Facade.Implementation version that the bootstrapper will use -->
    <!--<add key="FacadeImplementationVersion" value="4.8.0"/>-->

    <!-- Specify when the facade startup Window will launch. Allowed values are : Never, OnCtrlKeyPressed, Always. The default is OnCtrlKeyPressed -->
    <add key="OpenStartupWindowMode" value="Never" />
  </appSettings>
</configuration>
